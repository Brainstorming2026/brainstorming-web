---
import { Image } from 'astro:assets'
import chooseFirst from '@/assets/decorative/choose-first.webp'
import { clienteLogos } from '@/assets/clientes'
---

<!-- Por qué elegirnos -->
<section id="clientes" class="bg-[#f7f7f7]">
  <div class="mx-auto max-w-[1512px] px-6 py-16 text-center lg:px-24">
    <h2 class="font-heading text-3xl font-bold text-ink sm:text-[35px]">
      Por qué elegirnos
    </h2>
    <Image
      src={chooseFirst}
      alt="Top Web Development Companies — DesignRush"
      class="mx-auto mt-6 w-[152px]"
    />
  </div>
</section>

<!-- Nuestros clientes -->
<section class="bg-white">
  <div class="mx-auto max-w-[1512px] px-6 py-16 lg:px-24 lg:py-20">
    <h2 class="text-center font-heading text-3xl font-bold text-ink sm:text-[35px]">
      Nuestros clientes:
    </h2>

    <div
      id="clientes-grid"
      class="mt-14 grid grid-cols-3 items-center gap-x-8 gap-y-6 sm:grid-cols-5 lg:grid-cols-9"
    >
      {
        clienteLogos.map(c => (
          <div class="cliente-logo flex h-25 items-center justify-center">
            <Image
              src={c.image}
              alt={c.slug}
              loading="eager"
              class="max-h-30 w-full max-w-full object-contain"
            />
          </div>
        ))
      }
    </div>
  </div>
</section>

<script>
import { registerMotion, staggerGrid } from '@/lib/motion'

registerMotion(() => {
  const logos = document.querySelectorAll('#clientes-grid .cliente-logo')
  staggerGrid(logos, { variant: 'wipe' })
})
</script>
