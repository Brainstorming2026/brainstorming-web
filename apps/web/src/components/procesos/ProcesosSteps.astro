---
import { Image } from 'astro:assets'
import { Icon } from 'astro-icon/components'
import stepsImg from '@/assets/procesos/procesor-steps.png'
import { procesosBenefits } from '@/data'
---

<section class="bg-mist py-16">
  <div class="mx-auto max-w-[1140px] px-6 text-center lg:px-24">
    <h2 class="font-heading text-2xl font-bold text-dark sm:text-3xl">
      ¡Conoce el Proceso detrás de los procesos!
    </h2>
    <p class="mx-auto mt-4 max-w-2xl text-[15px] leading-6 text-dark-500">
      A lo largo de nuestra experiencia, hemos identificado las siguientes 9 etapas en la mejora continua
      de una organización.
    </p>
  </div>
</section>

<section class="bg-white py-20">
  <div class="mx-auto max-w-[1140px] px-6 lg:px-24">
    <Image
      src={stepsImg}
      alt="Las 9 etapas del proceso: evaluar viabilidad, empatizar con el cliente, empatizar con el equipo, analizar procesos, identificar herramientas digitales, implementar herramientas, mejorar experiencia, probar oportunidades de mejora e implementar mejoras"
      class="mx-auto h-auto w-full"
      widths={[560, 1140]}
      sizes="(min-width: 1140px) 1140px, 100vw"
      loading="lazy"
    />
  </div>
</section>

<section class="bg-white pb-16">
  <div class="mx-auto max-w-[1140px] px-6 lg:px-24">
    <h2 class="text-center font-heading text-2xl font-bold text-dark sm:text-3xl">
      ¿En qué puede esto ayudar a tu negocio?
    </h2>

    <div class="mt-10 grid gap-8 sm:grid-cols-2">
      <ul class="space-y-4">
        {
          procesosBenefits.left.map((text) => (
            <li class="flex items-start gap-3">
              <Icon name="sales-check" class="mt-1 h-3 w-auto flex-shrink-0 text-orange" />
              <span class="text-[15px] leading-6 text-dark-500">{text}</span>
            </li>
          ))
        }
      </ul>
      <ul class="space-y-4">
        {
          procesosBenefits.right.map((text) => (
            <li class="flex items-start gap-3">
              <Icon name="sales-check" class="mt-1 h-3 w-auto flex-shrink-0 text-orange" />
              <span class="text-[15px] leading-6 text-dark-500">{text}</span>
            </li>
          ))
        }
      </ul>
    </div>
  </div>
</section>
